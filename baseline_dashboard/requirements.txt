# Baseline dashboard (CLUE study control condition).
# Versions match the repo's existing .venv; no new dependencies were added.
streamlit>=1.55.0
pandas>=2.3.0
numpy>=2.2.0
altair>=6.0.0
pyarrow>=23.0.0
